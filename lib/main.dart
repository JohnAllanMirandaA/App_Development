import 'dart:convert';
import 'WelcomePage.dart';

import 'package:flutter/material.dart';
import 'package:shared_preferences/shared_preferences.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Booking App',
      theme: ThemeData(
        primarySwatch: Colors.blue,
      ),
      home: WelcomePage(),
    );
  }
}

// Welcome Page



// User Login Page



// Organizer Login Page


// Register Page
// User Dashboard
// User Dashboard





// Event Options Page




// Coordinator Model
// Organizer Dashboard
// Service Page

// Coordinator Registration Page

// Assume Coordinator class is already defined with `toJson()` method

// Assume Coordinator class is already defined with `toJson()` method


// Coordinator Details Page with Rating and Feedback

// CoordinatorListPage: Navigate to CoordinatorDetailsPage when clicking a coordinator

  


